package com.jahnvi.properties.interfaces;

public interface MediaPlayer{
    void start();
    void stop();

    // by mistake i defined MediaPlayer as a class, due to which it showed error , becuase then it's body has to be writtened there. but being mthods of interface , there's no need to write their body

}
